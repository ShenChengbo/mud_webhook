services:
  - type: web
    name: arkham-webhook
    runtime: go
    buildCommand: go build -tags netgo -ldflags '-s -w' -o app
    startCommand: ./app
    envVars:
      - key: PORT
        value: "8080"   # Render 会自动注入端口